# Workspace Rules

- **Binary Compilation**: Always build/recompile the standalone production binary (`dist/png2webp.exe`) using PyInstaller (`.\venv\Scripts\pyinstaller --clean -y png2webp.spec`) after any modification to the source code (`png2webp.py`).
